Given the following data structure, 

return a new array containing the same sub-arrays as the original 

but ordered logically 

according to the numeric value of 

the odd integers they contain.

```ruby
arr = [[1, 6, 7], [1, 4, 9], [1, 8, 3]]


arr.map do |sub_array| 
  sub_array.sort_by {|num| num.odd?}

[1, 6, 7].sort <=> [1, 4, 9].sort
=> 1
[1, 6, 7].sort <=> [1, 4, 9].sort  <=> [1, 8, 3].sort
SyntaxError: unexpected <=>
...7].sort <=> [1, 4, 9].sort  <=> [1, 8, 3].sort
...                            ^~~
[1, 4, 9].sort  <=> [1, 8, 3].sort
=> 1










arr = [[1, 6, 7], [1, 4, 9], [1, 8, 3]]

arr.sort_by{|idx| idx if idx.odd?}


Sorts enum using a set of keys generated by mapping the values in enum through the given block.



arr = [[1, 6, 7], [1, 4, 9], [1, 8, 3]]

arr.sort_by{|idx| [idx .odd?]}
  idx.map{|idx| idx if idx.odd?}

w{apple pear fig}.sort_by { |word| word    .length }
=> ["fig", "pear", "apple"]
#=> ["fig", "pear", "apple"]              

*************************************************JMX

arr = [[1, 6, 7], [1, 4, 9], [1, 8, 3]]

sort arr by the odd numbers in the the array by returning an array
containing only the odd numbers from the sub arrays 

[1, 6, 7].map {|elem| elem if elem.odd?}
=> [1, nil, 7]



arr.sort_by{|idx| idx.map(&:odd?) }
  idx.map{|idx| idx if idx.odd?}

arr.sort_by{|idx| (idx.map{|elem| elem if elem.odd?})}




arr = [[1, 6, 7], [1, 4, 9], [1, 8, 3]]

sort arr by the odd numbers in the the array by returning an array
containing only the odd numbers from the sub arrays 


arr = [[1, 6, 7], [1, 4, 9], [1, 8, 3]]

[1, 6, 7].map {|elem| elem if elem.odd?}
=> [1, nil, 7]
*************************************************JMX

sort arr by the odd numbers in the the array by returning an array
containing only the odd numbers from the sub arrays 


arr = [[1, 6, 7], [1, 4, 9], [1, 8, 3]]

arr.sort_by do |idx| 
              
                [1, 6, 7].map {|elem| elem if elem.odd?}
                => [1, nil, 7]


  idx.map{|elem| elem if elem.odd?}
end




arr.sort_by do |idx|
  idx.map{|elem| elem if elem.odd?}
end




























